console.error(
  "This script uses the old username/password pCloud authentication flow and is not used by WatchTogether."
);
console.error(
  "Use scripts/pcloud-token.js with a pCloud OAuth authorization code instead."
);
process.exit(1);
